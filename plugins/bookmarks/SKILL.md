# bookmarks

Bookmark management. Access via `/note save`, `/note bookmarks`, `/note unsave`.

## Commands (standalone, prefer /note variants)
- `/bookmark` → use `/note save`
- `/bookmarks` → use `/note bookmarks`
- `/unbookmark` → use `/note unsave`
