# finance_maintenance

Journal maintenance. Access via `/finance void`, `/finance validate`, `/finance fxsync`, `/finance summary`.

## Commands (standalone, prefer /finance variants)
- `/validate` → use `/finance validate`
- `/fxsync` → use `/finance fxsync`
- `/void` → use `/finance void`
- `/summary` → use `/finance summary`
